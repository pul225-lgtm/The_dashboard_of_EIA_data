# The_dashboard_of_EIA_data
These R codes use the ShinyApp frame to realized a function which can request data from the eia website and print the date versus generation graph. Including requesting data, showing the graph, exporting the the graph and data used.
